package com.${packageName}.service.major;

import com.${packageName}.vo.${className};
import com.${packageName}.vo.${className}Example;

import tp.ms.base.rest.resource.service.IMajorService;

public interface ${pkSuffix}MajorService extends IMajorService<${className}, ${className}Example> {

}
