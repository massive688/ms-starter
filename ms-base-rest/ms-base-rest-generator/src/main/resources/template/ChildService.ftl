package com.${packageName}.service.child;

import com.${packageName}.vo.${className};
import com.${packageName}.vo.${className}Example;

import tp.ms.base.rest.resource.service.IChildService;

public interface ${pkSuffix}Service extends IChildService<${className}, ${className}Example>{

}
